// Generated once per server process start, used to cache-bust static assets
// (client-bundle.js/output.css) so a fresh deploy never hydrates against a
// browser-cached bundle from a previous version, which causes hydration mismatches.
export const BUILD_ID = crypto.randomUUID();
